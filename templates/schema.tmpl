{{if .IsPrimitive -}}
{{.ReceiverName}} {{.GoType}}
{{- else if .IsObject -}}
{{.GoType}} struct {
  {{- range .Properties}}
  {{if .IsDefinedElsewhere -}}
  {{.ReceiverName}} {{.GoType}}
  {{- else -}}
  {{template "schema.tmpl" .}}
  {{- end -}}
  {{- end -}}
}
{{- else if .IsSlice -}}
{{.GoType}}
{{- end}}
